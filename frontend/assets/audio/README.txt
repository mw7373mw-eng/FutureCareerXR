Place audio files here: ambient.mp3 (background music loop), click.mp3 (button/interaction click), door.mp3 (door open sound), room_*.mp3 (per-room ambience).
These are already wired up in index.html (<audio> tags) and script.js (playSfx()) — just add files with these exact names and they will play automatically. Missing files fail silently so the demo still runs without them.
