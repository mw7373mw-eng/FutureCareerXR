Place real exhibit photos here (e.g. solar_panel.jpg, mri.jpg).
The app currently generates placeholder images on-the-fly in script.js (see generatePlaceholderImage()).
To use real images: in openObjectModal() inside script.js, replace the img.src line with e.g. img.src = 'assets/images/' + objectId + '.jpg';
